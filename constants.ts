// Constants for the application.

/** Idle flush for screen reader prompt lines (no trailing newline). */
export const PROMPT_FLUSH_MS = 250;

/** Debounce for chunk-by-chunk screen reader announcements. */
export const SR_CHUNK_DEBOUNCE_MS = 100;
